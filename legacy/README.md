# Legacy — proyecto original

Snapshot del proyecto tal como estaba antes de migrar al monorepo. **No tocar.**

- `app-cotizaciones-main/` → frontend en Create React App + MUI v5
- `server-master/` → backend Express + axios + cache manual

Se conserva como referencia mientras se reescriben las features en `apps/web` y `apps/api`.

Cuando todas las features estén migradas, esta carpeta se puede borrar.
